# 物理卡牌对战 — AI 入职手册

## 用户
产品负责人，初中物理教师。定方向、审物理、做决策。
代码全权交给 AI。偏好简洁交流，反感冗长解释。

## 项目
109 张赛博朋克风物理卡牌对战游戏，力/声/光/热/电五领域。
原生 HTML/CSS/JS，无框架。仓库 git@gitee.com:jopapa/physics-card-game.git

## 铁律

1.  cards.js 是唯一权威源。卡牌数据字段名必须与 cards.js 一致，禁止自创别名。
2.  涉及卡牌效果，严格从 cards.js 读取，禁止自行联想物理原理。
3.  改引擎（engine.js）后必须跑 test_all_cards.cjs，109/109 通过才能提交。
4.  任何代码修改：先 pull → 改 → 跑语法检查 → 跑全量测试 → 提交推送。
5.  收工最后一步：git add -A && git commit && git push，不需等提醒。
6.  卡牌外观决策须经模板确认，再写入 MEMORY.md 后实现。
7.  测试版和基线版代码分开存放；生成 .js 数据文件后须 node -c 语法校验。
8.  用外部浏览器测试，不用内部预览；讨论阶段先不改代码。
9.  卡牌配色按稀有度区分，非按领域；5 个领域符文；跨领域卡显示多符文。
10. 手牌上限 7；抽卡概率 普通70% 稀有25% 传说5%，十连保底稀有。
11. 同名卡升级：费用-1，伤害+10%。
12. 稀有度图标：普通银六边形、稀有金菱形、史诗紫四角星、传说橙八角星。
13. 有疑必问：不确定时及时提出，不等用户发现问题。
14. 自动化任务中只做只读观察者，生成报告不主动建议。

## 工作模式

- 模式 A（自动修）：纯正确性问题（bug/死代码/漏实现），直接修。
- 模式 B（提案）：涉及物理正确性、设计审美、有多种方案时，列表格让用户决策。
- 模式 C（自主推进）：多步骤任务有清晰终点，自行推进到检查点汇报。

优先级：
| P0 | 崩溃/无法玩 | 立即修，不问 |
| P1 | 功能错误 | 下一轮优先 |
| P2 | 体验/质量 | 积攒 5+ 项后列表格审批 |
| P3 | 打磨 | 有空再弄 |

## 资源

- `card_index.md` — 109 卡速查表
- `review_report.md` — 最新代码审查结果
- `PROJECT_STATUS.md` — 阶段规划 + 当前待办 + Loop 状态
- `test_all_cards.cjs` — 全量回归测试（改引擎后必跑）
- `combo_proposals.md` — Combo 扩充提案记录

## 每次开工

1. `git pull`
2. 读 `PROJECT_STATUS.md` → 「⏳ 当前待办」+ 「🔁 Loop 状态」
3. 读 `review_report.md`

## 每次收工

1. `git add -A && git commit -m "xxx" && git push`
2. 在 `PROJECT_STATUS.md` 底部追加 `> 📝 本次：xxx`
